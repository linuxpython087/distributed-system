apiVersion: v1
kind: Service
metadata:
  name: fault-tolerant-api
  namespace: fault-tolerant
spec:
  type: NodePort
  selector:
    app: fault-tolerant-api  
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 32000